{
  "family": "message",
  "taskRoleArn": "",
  "containerDefinitions": [{
    "name": "message",
    "image": "",
    "cpu": 256,
    "memory": 512,
    "essential": true,
    "portMappings": [{
      "hostPort": 0,
      "containerPort": 3000,
      "protocol": "tcp"
    }],
    "environment": [
      {
        "name": "NODE_ENV",
        "value": "production"
      }
    ]
  }],
  "networkMode": "bridge"
}
